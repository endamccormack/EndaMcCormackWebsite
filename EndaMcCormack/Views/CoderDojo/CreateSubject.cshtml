@model EndaMcCormack.Models.Subject

@{
    ViewBag.Title = "CreateSubject";
    Layout = "~/Views/Shared/_CoderDojo.cshtml";
}

<h2>CreateSubject</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Subject</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.subject1)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.subject1)
            @Html.ValidationMessageFor(model => model.subject1)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
